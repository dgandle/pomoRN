const timerStatus = {
    STOPPED: 'stopped',
    ACTIVE: 'active',
    RESTING: 'resting',
    PAUSED: {
        ACTIVE: 'paused.active',
        RESTING: 'paused.resting'
    }
}

export default timerStatus;